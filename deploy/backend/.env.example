# Скопируйте в .env на сервере бэкенда (10.10.213.15).
# Значения должны совпадать с deploy/db/.env на сервере 10.10.213.48.

POSTGRES_USER=campusmadi
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD
POSTGRES_HOST=10.10.213.48
POSTGRES_PORT=5432
POSTGRES_DB=project_db

# Секрет для подписи JWT. Сгенерируйте, например:
#   python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=CHANGE_ME_RANDOM_LONG_SECRET
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
